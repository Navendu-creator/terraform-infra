apiVersion: v1
kind: Service
metadata:
  name: wazuh-dashboard
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: wazuh-dashboard
  ports:
    - port: 5601
      targetPort: 5601
