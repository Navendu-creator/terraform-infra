# Provider configuration is expected to be done at the environment level
# (so different environments can use different regions/accounts).
# Example provider block (uncomment & customize in envs if desired):

# provider "aws" {
#   region = var.region
#   # Use profiles, assume_role, or separate credentials via environment variables or CI
# }
